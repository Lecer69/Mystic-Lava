buildscript {
    repositories {
        maven { url uri("https://plugins.gradle.org/m2/") }
    }
}

plugins {
    id 'java'
}

repositories {
    mavenLocal()
}

dependencies {
    implementation project(':api')
}

jar {
    archiveFileName = "MysticLava-${version}.jar"
    destinationDirectory = file("$rootDir/output")
}